<section id="contact" class="contact-section">
    <p class="mono accent">READY TO SHIP?</p>
    <a href="mailto:hello@waj.dev" class="cta-big">Let's build somewhat <br>great together.</a>

    <!-- Contact Form -->
    <div style="max-width: 500px; margin: 4rem auto 0 auto; text-align: left;">
        <form onsubmit="event.preventDefault(); alert('Message sent (demo)!');"
            style="display: flex; flex-direction: column; gap: 1rem;">
            <div style="display: flex; gap: 1rem;">
                <input type="text" placeholder="NAME"
                    style="flex:1; background: var(--surface); border: 1px solid var(--line); color: var(--text-main); padding: 1rem; font-family: var(--font-mono);">
                <input type="email" placeholder="EMAIL"
                    style="flex:1; background: var(--surface); border: 1px solid var(--line); color: var(--text-main); padding: 1rem; font-family: var(--font-mono);">
            </div>
            <textarea placeholder="MESSAGE_CONTENT" rows="4"
                style="background: var(--surface); border: 1px solid var(--line); color: var(--text-main); padding: 1rem; font-family: var(--font-mono); resize: vertical;"></textarea>
            <button type="submit"
                style="background: var(--accent); color: var(--bg); border: none; padding: 1rem; font-family: var(--font-mono); font-weight: bold; cursor: pointer; text-transform: uppercase;">
                Transmit Message ->
            </button>
        </form>
    </div>

    <div style="margin-top: 6rem; color: var(--text-dim); font-size: 0.9rem;" class="mono">
        HYDERABAD, INDIA (UK-ORIENTED) · REMOTE FIRST
    </div>
    <div style="margin-top: 2rem; border-top: 1px solid var(--line); padding-top: 2rem; display: flex; justify-content: space-between;"
        class="mono dim">
        <span>© 2025 WAJ.DEV</span>
        <span>SYSTEM_READY</span>
    </div>
</section>
