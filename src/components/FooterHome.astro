<section id="contact" class="contact-section">
  <p class="mono accent">READY TO SHIP?</p>
  <a href="mailto:hello@waj.dev" class="cta-big"
    >Let's build something <br />great together.</a
  >

  <div class="contact-form-container">
    <form
      onsubmit="event.preventDefault(); alert('Message sent (demo)!');"
      class="contact-form"
    >
      <div class="form-row">
        <input type="text" placeholder="NAME" class="form-input" />
        <input type="email" placeholder="EMAIL" class="form-input" />
      </div>
      <textarea
        placeholder="MESSAGE_CONTENT"
        rows="4"
        class="form-input"
        style="resize: vertical;"></textarea>
      <button type="submit" class="submit-btn"> Transmit Message -&gt; </button>
    </form>
  </div>

  <style>
    .contact-form-container {
      max-width: 500px;
      margin: 4rem auto 0 auto;
      text-align: left;
    }
    .contact-form {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }
    .form-row {
      display: flex;
      gap: 1rem;
    }
    .form-input {
      background: var(--surface);
      border: 1px solid var(--line);
      color: var(--text-main);
      padding: 1rem;
      font-family: var(--font-mono);
      width: 100%;
    }
    .submit-btn {
      background: var(--accent);
      color: var(--bg);
      border: none;
      padding: 1rem;
      font-family: var(--font-mono);
      font-weight: bold;
      cursor: pointer;
      text-transform: uppercase;
    }

    @media (max-width: 600px) {
      .form-row {
        flex-direction: column;
      }
    }
  </style>

  <div
    style="margin-top: 6rem; color: var(--text-dim); font-size: 0.9rem;"
    class="mono"
  >
    HYDERABAD, INDIA · REMOTE FIRST
  </div>
  <div
    style="margin-top: 2rem; border-top: 1px solid var(--line); padding-top: 2rem; display: flex; justify-content: space-between;"
    class="mono dim"
  >
    <span>© 2025 WAJ.DEV</span>
    <span>SYSTEM_READY</span>
  </div>
</section>
